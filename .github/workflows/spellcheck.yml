name: Spellcheck

on:
  pull_request:

jobs:
  spellcheck:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Run CSpell
        uses: streetsidesoftware/cspell-action@v8
        with:
          config: './cspell.json'
