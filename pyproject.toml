[project]
name = "ksana-tests"
version = "0.0.0"
requires-python = ">=3.12"

[dependency-groups]
dev = ["pytest>=9", "ruff>=0.9"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
target-version = "py312"

[tool.ruff.lint]
select = ["ANN", "E", "F", "I", "RUF"]
